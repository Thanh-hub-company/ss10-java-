public class Dog extends Animal {

    // Constructor nhận vào tham số name
    public Dog(String name) {
        // Gán giá trị cho thuộc tính name kế thừa từ lớp cha Animal
        this.name = name;
    }
}